<!DOCTYPE html>
<html>
  <head>
    <title>Book</title>
  </head>
  <body>
    <form method="POST" action="/books/<%= it.book.id %>">
      <label for="Book name">Book name:</label>
      <input type="text" id="bookName" name="bookName" value="<%= it.book.bookName %>" /><br/>
      <label for="Number of pages">Number of pages:</label>
      <input type="text" id="numberOfPages" name="numberOfPages" value="<%= it.book.numberOfPages %>" /><br/>
      <label for="ISBN">ISBN:</label>
      <input type="text" id="isbn" name="isbn" value="<%= it.book.isbn %>" /><br/>
      <input type="submit" value="Update" />
    </form>

    <p><a href="/books">Back to books</a></p>
  </body>
</html>